const fs = require('fs');
const content = fs.readFileSync('src/app/admin/page.tsx', 'utf-8');

console.log('trilhas:', content.indexOf(mainTab === 'trilhas' && ());
console.log('clientes:', content.indexOf(mainTab === 'clientes' && ());
console.log('reservas:', content.indexOf(mainTab === 'reservas' && ());
console.log('financas:', content.indexOf(mainTab === 'financas' && ());
